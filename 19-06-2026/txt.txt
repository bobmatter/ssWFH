SERVER_BASE = "https://api357.cf.adapid.link"

Your APP_DISPLAY_NAMES question
You're right that it doesn't fully make sense as a fixed dict once app_name is dynamic — but it's not pointless either, it's just incomplete. It's a display-name lookup with a fallback: if app_name is a known key, show the friendly name; otherwise fall back to the raw exe name. That part of the logic is fine and doesn't need touching. The real fix needed is making sure app_name itself is read correctly per-request (Bug 2) rather than relying on a stale global — once that's fixed, APP_DISPLAY_NAMES.get(app_name, app_name) does exactly what it should: nice name for known apps, raw name for anything new in LOCKED_APPS you haven't added a display label for yet.
Here's the corrected blueprint:Ran 2 commands, created a file, read a fileRan 2 commands, created a file, read a fileOne thing your behavior5.html snippet needs that I want to flag explicitly: it must actually render value="{{ app_name }}" and value="{{ username }}" into those two hidden inputs (you showed this for app_name/username already — good), but make sure behavior4.html (the re-render-on-error template) has the same two hidden fields. Looking at your rerender() function, it passes app_name and username into behavior4.html's template context, but if that template doesn't actually echo them back into hidden <input> tags, then a retry-after-error submission will arrive at /complete with app_name="" and username="" — silently breaking exactly the bug we just fixed, just one hop later. Worth a quick check of that template if you have it handy.
Also, separately — not a bug, just a note: I noticed the original snippet had is_legitimate = True hardcoded right before the failure check, which would make the if not is_legitimate: branch permanently dead code (always succeeds, behaviour-API result notwithstanding). I removed that line in the fixed version since it looked like leftover debugging, but flagging it in case it was intentional for testing — if you want it back in for now, it's a one-line add right before if not is_legitimate:.